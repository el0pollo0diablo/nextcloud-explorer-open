# v0.2.1

- Removes the previous domain-based namespace from the Firefox extension ID and native messaging host name.
- Uses `@el0pollo0diablo-nextcloud-explorer-open` as the internal Firefox extension ID.
- Uses `io.github.el0pollo0diablo.nextcloud_explorer_open` as the Windows native messaging host name.
